pool:
  vmImage: 'ubuntu-16.04' # Hosted Ubuntu 1604

stages:
  - stage: Git
    jobs:
      - job: 'Credentials'
        steps:
          - bash: 'echo This is Credentials Step'
  - stage: Clone
    jobs:
      - job: 'ConePull'
        steps:
          - bash: 'echo This is Credentials Step'

